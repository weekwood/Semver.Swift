language: objective-c

before_install:
- export LANG=en_US.UTF-8
- brew update
- if brew outdated | grep -qx xctool; then brew upgrade xctool; fi

script:
- xcodebuild test -project SemverSwift.xcodeproj -scheme SemverSwift -destination 'platform=iOS Simulator,name=iPhone 6s,OS=9.3'
